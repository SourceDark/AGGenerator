hacl(H0, H1, httpProtocol, httpPort).
networkService(H1, httpd, httpProtocol, httpPort, Apache).
vulExists(H1, ’CVE-2006-3747’, httpd, remoteExploit,
privEscalation).
remote exploit of a server program.
execCode(H1, Apache).
hacl(H1, H3, dbProtocol, dbPort).
multi-hop access.
netAccess(H1, H3, dbProtocol, dbPort).
vulExists(H3, ’CVE-2009-2446’, mysqld, remoteExploit, privEscalation).
remote exploit of a server program.
execCode(H3, Apache).
vulExists(H3, ’CVE-2004-0495’, linux-kernel,
localExploit, privEscalation).
local exploit of OS kernel.
execCode(H3, root).
hacl(H0, H2, sshProtocol, sshPort).
direct network access.
netAccess(H0, H2, sshProtocol, sshPort).
networkService(H2, sshd, sshProtocol, sshPort, SSH).
vulExists(H2, ’CVE-2002-0640’, sshd, remoteExploit,
privEscalation).
remote exploit of a server program.
execCode(H2, SSH).
hacl(H1, H2, httpProtocol, httpPort).
multi-hop access.
netAccess(H2, H1, httpProtocol, httpPort) 29. remote exploit of a server program